{
  "name": "dopa-mahjong",
  "private": true,
  "workspaces": [
    "shared",
    "web"
  ],
  "scripts": {
    "test": "npm run test --workspace=shared",
    "dev": "npm run dev --workspace=web",
    "build": "npm run build --workspace=web"
  }
}
